diff --git a/meson.build b/meson.build
index ac70663..c831dd9 100644
--- a/meson.build
+++ b/meson.build
@@ -230,8 +230,8 @@ pkgconfig.generate(
 
 # libxkbcommon-x11.
 if get_option('enable-x11')
-    xcb_dep = dependency('xcb', version: '>=1.10', required: false)
-    xcb_xkb_dep = dependency('xcb-xkb', version: '>=1.10', required: false)
+    xcb_dep = dependency('xcb', version: '>=1.10', required: false, static: get_option('use-static-xcb'))
+    xcb_xkb_dep = dependency('xcb-xkb', version: '>=1.10', required: false, static: get_option('use-static-xcb'))
     if not xcb_dep.found() or not xcb_xkb_dep.found()
         error('''X11 support requires xcb-xkb >= 1.10 which was not found.
 You can disable X11 support with -Denable-x11=false.''')
@@ -442,9 +442,9 @@ if get_option('enable-x11')
     executable('interactive-x11', 'test/interactive-x11.c', dependencies: x11_test_dep)
 endif
 if get_option('enable-wayland')
-    wayland_client_dep = dependency('wayland-client', version: '>=1.2.0', required: false)
-    wayland_protocols_dep = dependency('wayland-protocols', version: '>=1.12', required: false)
-    wayland_scanner_dep = dependency('wayland-scanner', required: false, native: true)
+    wayland_client_dep = dependency('wayland-client', version: '>=1.2.0', required: false, static: get_option('use-static-wayland'))
+    wayland_protocols_dep = dependency('wayland-protocols', version: '>=1.12', required: false, static: get_option('use-static-wayland'))
+    wayland_scanner_dep = dependency('wayland-scanner', required: false, native: true, static: get_option('use-static-wayland'))
     if not wayland_client_dep.found() or not wayland_protocols_dep.found() or not wayland_scanner_dep.found()
         error('''The Wayland demo programs require wayland-client >= 1.2.0, wayland-protocols >= 1.7 which were not found.
 You can disable the Wayland demo programs with -Denable-wayland=false.''')
diff --git a/meson_options.txt b/meson_options.txt
index 0e166b1..7f49f6a 100644
--- a/meson_options.txt
+++ b/meson_options.txt
@@ -56,3 +56,15 @@ option(
     value: true,
     description: 'Enable support for Wayland utility programs',
 )
+option(
+    'use-static-xcb',
+    type: 'boolean',
+    value: false,
+    description: 'Try to use static version of libxcb and libxcb-xkb when link with xkbcommon-x11',
+)
+option(
+    'use-static-wayland',
+    type: 'boolean',
+    value: false,
+    description: 'Try to use static version of wayland-{client,protocol,scanner} when link with interactive-wayland',
+)
